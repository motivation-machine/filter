language: node_js

build:
  ci:
    - docker build -t="scriptnull/mm-filter:$BRANCH.$BUILD_NUMBER" .
    - docker push scriptnull/mm-filter:$BRANCH.$BUILD_NUMBER

integrations:
  hub:
    - integrationName: Vishnu's Docker
      type: docker

# integrations:
#   key:
#     - integrationName: mm-keys
#       type: ssh-key
