language: node_js

build:
  ci:
    - ./deploy.sh

integrations:
  hub:
    - integrationName: Vishnu's Docker
      type: docker

  key:
    - integrationName: motivation-machine
      type: ssh-key
